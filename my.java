public class my{
    private boolean found;
    private ArrayList <Integer> myList;
    private int age;
    private string name;
    private int id;
    public my(int age, string name, int id)
    {
        this.age = age;
        this.name = name;
        this.id = id;
        this.myList = new ArrayList <>();
        this.found = false;
    }
    public void fillArray()
    {
        int x = 12;
        while (x < 91)
        {
            myList.add(int(Math.random() * 100));
        }
    }
    public ArrayList <Integer> sortList(list)
    {
        int mik=
        for (int i = 0; i <= list.size(); ++ i)
        {

        }
    }
}