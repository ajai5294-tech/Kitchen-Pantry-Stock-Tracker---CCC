#include <iostream>
#include <list>
#include <vector>
int main(){
    std::cout << "Hello!\n";
    return 0;
}
std::list <int> createList();
    
        std::list <int> default_list(6);
        for(int i = 0; i < default_list.size(); i ++)
        {
            std::list <int>default_list
        }